json.extract! sklad, :id, :oldid, :category, :name, :counttt, :nottte, :image, :price, :min_count, :count_zakaz, :save_place, :artikul, :name_pdf, :draw_pdf, :name_dxf, :draw_dxf, :value_types, :created_at, :updated_at
json.url sklad_url(sklad, format: :json)
